#include <iostream>
#include <string>

using namespace std;

int main() {
    // Define the string
    string phrase = "Vive la plateforme !";
    
    // Calculate the length of the string
    size_t length = phrase.length();
    
    // Display the message with the length of the string
    cout << "The length is " << length << endl;

    return 0;
}
